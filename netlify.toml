[build]
  # This tells Netlify where your functions are
  functions = "netlify/functions"
  # This tells Netlify what to publish - your html files
  publish = "."

# This fixes routing so quiz.html works when you refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200